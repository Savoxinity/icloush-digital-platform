services:
  icloush-admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: icloush-admin
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "${ADMIN_PORT:-3000}:3000"

# LAB H5 Preview Notes

- 预览地址 `/lab` 已正常打开，首屏呈现暗色 Hero、AP/FC/Orbital Assets 导航以及 `[ REQUEST ACCESS ]` 与 `[ INITIATE PROTOCOL ]` CTA。
- Hero 标题已显示为 `ATMOSPHERIC PURIFICATION`，但自动提取文本中两个单词连在一起；视觉截图中两行排版正常。
- 右侧状态面板当前显示 `LOCAL FALLBACK`，说明联系配置请求在浏览器预览中落到了默认数据来源，而不是数据库配置。
- 指标面板在预览中显示 `PUBLIC PRODUCTS 2 / PIPELINE ORDERS 1 / LEAD CAPTURE 1`，说明页面已成功接入真实站点摘要查询。
- 页面下方 AP、Olfactory Archive 与 FC 预留模块已在抽取文本中可见，结构完整。
